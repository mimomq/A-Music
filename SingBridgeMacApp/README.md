# SingBridge Mac App

Open this folder in Xcode when working only on the Mac receiver.

Use this target and destination:

- Scheme/product: `SingBridgeMac`
- Destination: `My Mac`

This package intentionally excludes the iPhone executable target.

The source folders are linked back to the main repository so edits stay in one codebase.
